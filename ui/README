UI Service hosts the UI files using a dev server.
This just talks to the API Server.

Archive the repo `git archive -o source.tar HEAD`
Upload to s3 `aws s3 cp source.tar s3://constellation-prior/tmp/source.tar`
